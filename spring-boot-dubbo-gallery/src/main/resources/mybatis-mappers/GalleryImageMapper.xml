<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.open.com.open.springqianbailu.dao.gallery.GalleryImageMapper">
  <resultMap id="BaseResultMap" type="com.open.springqianbailu.model.table.gallery.GalleryImage">
    <result column="id" jdbcType="INTEGER" property="id" />
    <result column="gallery_id" jdbcType="INTEGER" property="gallery_id" />
    <result column="src" jdbcType="VARCHAR" property="src" />
    <result column="title" jdbcType="VARCHAR" property="title" />
  </resultMap>

  <sql id="Base_Column_List">
    id, gallery_id,src, title
  </sql>

  <select id="selectById" resultMap="BaseResultMap" parameterType="hashmap" >
    select
    <include refid="Base_Column_List" />
    from gallery_image
    where gallery_id = #{galleryId}
  </select>

  <select id="selectImage" resultMap="BaseResultMap" parameterType="hashmap" >
    select
    <include refid="Base_Column_List" />
    from gallery_image
    where gallery_id = #{galleryId} and page = #{page}
  </select>

  <delete id="dropTable">
    drop table if exists gallery_image
  </delete>

  <insert id="createTable">
    CREATE TABLE `gallery_image` (
    `id` int(11) NOT NULL AUTO_INCREMENT,
    `gallery_id` int(11) NOT NULL,
    `src` varchar(255) COLLATE utf8_bin DEFAULT NULL,
    `title` varchar(255) COLLATE utf8_bin DEFAULT NULL,
    PRIMARY KEY (`id`,`gallery_id`) USING BTREE
  ) ENGINE=InnoDB   AUTO_INCREMENT=0 DEFAULT CHARSET=utf8 COLLATE=utf8_bin;
  </insert>

  <insert id="insert" useGeneratedKeys="true" keyProperty="id" parameterType="com.open.springqianbailu.model.table.gallery.GalleryImage">
    insert into gallery_image (id, gallery_id, src, 
      title)
    values (#{id,jdbcType=INTEGER}, #{gallery_id,jdbcType=INTEGER}, #{src,jdbcType=VARCHAR},
      #{title,jdbcType=VARCHAR})
  </insert>
</mapper>